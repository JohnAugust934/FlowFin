---
stage: 6
task: 2
title: "Guia de Deploy Passo a Passo (DevOps & Docs Agent)"
agent: devops-docs-agent
status: Partial
important_findings: true
compatibility_issues: false
---

# Task 6.2 - Guia de Deploy Passo a Passo (leigo)

## Summary
Entregue o guia de deploy na Hostinger via GitFlow/GitHub, do push à publicação, em PT-BR e
linguagem leiga, refletindo a configuração da 6.1 e o deploy de teste já realizado. Status
**Partial** porque o critério final — "o usuário consegue seguir o guia e publicar" — é uma
**validação guiada que depende de ação do usuário** (deploy real) e ainda não foi executada.

## Details
- **Documento `docs/02-guia-deploy.md`** com passo a passo numerado: pré-requisitos (SSH, SSL),
  ativação do SSL, document root → `public`, criação do banco MySQL, build local + publicação
  na `main` levando `public/build` (com `git add -f`), clone/pull via SSH, `composer install
  --no-dev --optimize-autoloader`, `.env` + `key:generate`, `migrate --force`,
  `config/route/view:cache`, permissões `chmod 775`, SMTP, e o **cron único**.
- Reflete o **fluxo GitFlow**: trabalho integrado em `develop` → publica na `main` → servidor
  baixa a `main`. Remoto já conectado: `https://github.com/JohnAugust934/FlowFin`.
- Seção **"Atualização"** (deploys seguintes) enxuta, e tabela de **"Armadilhas reais"** com os
  sintomas/causas/soluções aprendidos no teste (HTTPS, `public/build`, `storage:link`/`exec()`,
  document root, config em cache, cron, SMTP).
- Opção do usuário de teste do seeder (`TestDataSeeder` → teste@flowfin.com.br/senha1234) para
  validar sem depender do SMTP.
- Criado também `docs/README.md` (índice dos três documentos + atalhos).

## Output
- Criado: `docs/02-guia-deploy.md`
- Criado: `docs/README.md`
- Commit em `feature/deploy-docs`: `79d3366`

## Validation
- Conferência cruzada com a 6.1 (cada passo do guia corresponde a um item da configuração de
  produção) e com as lições do deploy de teste.
- **Pendente (validação guiada):** o usuário precisa **seguir o guia e publicar** de fato para
  fechar o critério de aceite. Passos que exigem **ação do usuário**: ativar SSL, criar banco,
  criar caixa SMTP, criar o cron, e **push para `main`** (deploy de produção).

## Issues
None de implementação. O fechamento depende da execução guiada pelo usuário (ver Validation).

## Important Findings
- O deploy de produção envolve **push para `main`** e ações de painel (SSL/cron/SMTP) — pontos
  em que o Manager deve **pausar e orientar o usuário**. Marcado como Partial até a validação
  guiada confirmar a publicação.
