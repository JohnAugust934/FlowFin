{{-- Ícone do FlowFin (sem texto). Use a classe para dimensionar (ex.: h-8 w-8). --}}
<img src="{{ asset('img/brand/icon_flowfin.svg') }}" alt="FlowFin" {{ $attributes->merge(['class' => 'h-8 w-8']) }}>
