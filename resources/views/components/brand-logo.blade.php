{{-- Logo completo do FlowFin (marca + texto). Use a classe para dimensionar (ex.: h-10). --}}
<img src="{{ asset('img/brand/logo_flowfin.svg') }}" alt="FlowFin" {{ $attributes->merge(['class' => 'h-10 w-auto']) }}>
